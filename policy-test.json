{
  "Policy": "KRYPT Token TEST Policy",
  "Description": "This policy is a simple test to troubleshoot an issue",
  "PolicyType": "open",
  "CallingFunctions": [
    {
      "name": "transfer(address to, uint256 value)",
      "functionSignature": "transfer(address to, uint256 value)",
      "encodedValues": "address to, uint256 value"
    }
  ],
  "ForeignCalls": [],
  "MappedTrackers": [],
  "Trackers": [],
  "Rules": [
    {
      "Name": "Will Revert",
      "Description": "This rule will always revert",
      "condition": "1 == 1",
      "positiveEffects": ["revert(\"force failure\")"],
      "negativeEffects": ["revert(\"this should never happen\")"],
      "callingFunction": "transfer(address to, uint256 value)"
    }
  ]
}
